#include <stdio.h>
#include <stdlib.h>
#include "stack.h"

void markPos(struct pos *pos) 
{
    pos->pheremone = true;
}

void move_F(struct pos *pos) {
    struct pos.x += 1;
}

void move_B(struct pos *pos) {

}